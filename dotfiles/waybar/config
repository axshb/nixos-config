{
    "layer": "top",
    "position": "top",
    "height": 45, 
    "margin-top": 8,
    "margin-left": 15,
    "margin-right": 15,

    "modules-left": ["hyprland/workspaces", "hyprland/window"],
    "modules-center": ["clock"],
    "modules-right": ["tray", "custom/nvidia", "cpu", "memory"],

    "hyprland/workspaces": {
        "format": "[{name}]",
        "on-click": "activate",
        "all-outputs": true
    },

    "hyprland/window": {
        "format": "{title}",
        "max-length": 30,
        "separate-outputs": true
    },

    "clock": {
        "format": "{:%a %d %b | %H:%M:%S}",
        "interval": 1,
        "tooltip-format": "<tt>{calendar}</tt>"
    },

    "custom/nvidia": {
        "exec": "nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader,nounits",
        "format": "GPU:{}%",
        "interval": 5,
        "tooltip": false
    },

    "cpu": { "format": "CPU:{usage}%" },
    "memory": { "format": "MEM:{percentage}%" },

    "tray": {
        "icon-size": 20,
        "spacing": 10
    }
}