# ==============================================================================
# OPENER DEFINITIONS
# ==============================================================================
[opener]
edit = [
	{ run = 'code "$@"', block = true, desc = "VS Code" }
]

view = [
	{ run = 'qimgv "$@"', orphan = false, desc = "qimgv" }
]

play = [
	{ run = 'mpv "$@"', orphan = false, desc = "qimgv" }
]

docs = [
	{ run = 'zathura "$@"', orphan = false, desc = "Zathura" }
]

[open]
prepend_rules = [
	# dirs (opens a whole folder in zed with O)
	{ name = "*/", use = [ "edit", "open" ] },

	{ name = "*.nix", use = "edit" },
	{ mime = "text/*", use = "edit" },
	{ mime = "application/{json,javascript,x-shellscript}", use = "edit" },
	{ name = "*.{yml,yaml,toml,conf,sh,py,lua,md,csv,rs,go,c,cpp,h}", use = "edit" },

	{ mime = "image/*", use = "view" },
	{ name = "*.svg", use = "view" },

	{ mime = "{video,audio}/*", use = "play" },

	{ mime = "application/pdf", use = "docs" },
	{ name = "*.{docx,xlsx,pptx}", use = "docs" },
]
